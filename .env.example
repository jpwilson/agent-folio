# =============================================================================
# Agent-Folio Configuration
# =============================================================================
# Copy this file to .env and fill in the values.
# At minimum you need: DATABASE_URL, JWT_SECRET, and one LLM API key.

# --- Backend Connections ---
# Ghostfolio (primary — stocks, ETFs, mutual funds)
GHOSTFOLIO_URL=http://localhost:3333
JWT_SECRET=your-jwt-secret-here

# Rotki (optional — crypto portfolio tracker)
# Set these if you want to connect Rotki as an additional backend.
# ROTKI_URL=http://localhost:8084
# ROTKI_USERNAME=your-rotki-username
# ROTKI_PASSWORD=your-rotki-password

# --- LLM API Keys (at least one required) ---
# OpenAI (for GPT-4o, GPT-4o-mini, etc.)
OPENAI_API_KEY=your-openai-api-key

# Anthropic (optional — for Claude models)
ANTHROPIC_API_KEY=

# OpenRouter (optional — bring your own key, access 100+ models)
# Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=

# --- Default LLM Settings ---
DEFAULT_SDK=litellm
DEFAULT_MODEL=gpt-4o-mini

# --- Database ---
# PostgreSQL connection string. Can share the Ghostfolio Postgres instance.
DATABASE_URL=postgresql://user:password@localhost:5432/ghostfolio-db

# --- Observability (optional) ---
# Langfuse — tracing, latency monitoring, cost analysis
# Sign up at https://langfuse.com
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://us.cloud.langfuse.com

# --- Server ---
HOST=0.0.0.0
PORT=8000

# --- Grader (optional) ---
# Used for automated evaluation via external grading service
GRADER_TOKEN=
